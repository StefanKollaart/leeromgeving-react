const lessons = [
  {
    lessonNumber: 1,
    title: "Pedicure les 1",
    video: "http://www.google.nl",
    tekst: "Hello World",
  }
]

export default (state = lessons, { type, payload } = {}) => {
  return state
}
